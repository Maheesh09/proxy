# Upload ONLY what Cloud Build needs
# DO NOT ignore the target/ directory since we need the JAR

# Source and IDE
src/
pom.xml
.idea/
.vscode/
*.iml

# Tools
maven-dist/

# Logs
*.log
*.tmp

# Git
.git/

# Keep: Dockerfile, target/proxymaze-1.0.0.jar
# Note: target/*.jar.original and target/classes are excluded below
target/classes/
target/generated-sources/
target/generated-test-sources/
target/maven-archiver/
target/maven-status/
target/test-classes/
target/*.jar.original
target/surefire-reports/
